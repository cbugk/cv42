%% -- TOGGLES --

% -- banner placement
\newtoggle{togIsRightPlacementBanner}
\toggletrue{togIsRightPlacementBanner}
\togglefalse{togIsRightPlacementBanner}
%

% -- banner/doc columns's vertical seperator line
\newtoggle{togDrawLineVerticalBanner}
\toggletrue{togDrawLineVerticalBanner}
%\togglefalse{togDrawLineVerticalBanner}
%

% -- within banner seperator for personal info and the rest
\newtoggle{togDrawLineHorizontalBanner}
\toggletrue{togDrawLineHorizontalBanner}
%\togglefalse{togDrawLineHorizontalBanner}
%